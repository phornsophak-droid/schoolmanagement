node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local-only tooling and personal helper scripts
.claude/
Push to GitHub.bat

# One-off maintenance backups/scripts (cloud dedupe, etc.)
_backup_*.json
_*.mjs

# Local source files (templates / rosters), not for the app build
certificate.docx
*.xlsx

# Office lock files
~$*
